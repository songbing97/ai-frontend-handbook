{
  "scripts": {
    "dev": "vitepress dev docs NODE_ENV=development",
    "build": "vitepress build docs NODE_ENV=product && git config --global user.email 1422254429@qq.com && git config --global user.name 宋承亮 && touch ./docs/.vitepress/dist/.nojekyll && gh-pages -d ./docs/.vitepress/dist/ -t true",
    "preview": "vitepress preview docs"
  },
  "dependencies": {
    "@vue/repl": "^1.4.0",
    "vitepress": "^1.0.0-alpha.61",
    "vue": "^3.2.47"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
