A: Transform has the following functions: translate(), rotate(), scale(), skew(), and matrix(). Among them, matrix() is a combination of translate, rotate, and scale functions.
A: TransForm一共有以下几种函数: translate() / rotate() / scale() / skew() / matrix()， 其中matrix是translate、rotate、scale的叠加。
